CREATE TABLE IF NOT EXISTS roles ( id int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, name varchar(255) NOT NULL UNIQUE );
INSERT INTO Roles (id, name) VALUES (1, 'Member') ON CONFLICT DO NOTHING ;
INSERT INTO Roles (id, name) VALUES (2, 'Admin') ON CONFLICT DO NOTHING ;
CREATE TABLE IF NOT EXISTS users (
    id int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY ,
    username varchar(255) NOT NULL UNIQUE,
    password varchar(255) NOT NULL,
    role_id int 
);
